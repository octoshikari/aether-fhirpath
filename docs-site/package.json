{
  "name": "docs-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build:wasm": "cd ../fhirpath-wasm && npm run build",
    "copy:wasm": "mkdir -p public/pkg && cp ../fhirpath-wasm/pkg/* public/pkg/",
    "prebuild": "npm run build:wasm && npm run copy:wasm",
    "build": "PUBLIC_BASE_URL=/aether-fhirpath astro build --base /aether-fhirpath ",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.35.0",
    "astro": "^5.6.1",
    "astro-favicons": "^3.1.5",
    "sharp": "^0.34.2"
  }
}
